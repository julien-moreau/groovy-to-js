const operators = {
    '-': 'subtract',
    '-=': 'subtract',
    '+': 'add',
    '+=': 'add'
};

const keywords = {
    'def': 'var'
};

const functions = {
    array: {
        'add': 'push'
    }
}

const properties = {
    array: {
        'size': 'length'
    }
}

export {
    operators,
    keywords,
    functions,
    properties
}
